Classroom Info
<%= render  partial: 'classroom', locals:{classroom: @classroom} %>

<%= @classroom.students.each do |student_obj| %>
  <%= render  partial: 'students/student', locals:{student: student_obj} %>
<% end %>
